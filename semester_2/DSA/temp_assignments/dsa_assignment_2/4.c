#include <stdio.h>
#include <stdlib.h>

#define MAX_SIZE 100
struct Stack {
    int arr[MAX_SIZE];
    int top;
};

void enque(struct Stack *s1){

}

int main(){
    struct Stack *s1 = (struct Stack*)malloc(sizeof(struct Stack));
}