#include <stdio.h>

enum weekdays{
    Monday,
    Tuesaday,
    Wednesday,
    Thursday,
    Friday,
    Saturday,
    Sunday
};

int main(){
enum weekdays a1;
a1 = Tuesaday;
printf("%d\n", a1);
return 0;
}