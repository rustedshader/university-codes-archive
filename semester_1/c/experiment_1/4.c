#include <stdio.h>

int main()
{
    int a, b;
    printf("Enter the first number\n");
    printf("Enter the second number\n");
    scanf("%d %d", &a, &b);
    printf("Sum of the numbers is %d\n", a + b);
    return 0;
}